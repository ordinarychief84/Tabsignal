# Secrets
.env
.env.local
.env*.local
credentials.json
token.json

# Node / Next.js
node_modules/
.next/
*.tsbuildinfo
next-env.d.ts
.vercel/
dist/
*.log

# SwiftPM build output (sdk/tabcall-wear/swift compile checks)
.build/

# Temp / intermediates
.tmp/

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.pytest_cache/

# OS / editor
.DS_Store
.vscode/
.idea/
.vercel

# Claude Code harness — per-session worktrees + runtime state. These
# accumulate fast (multi-GB across worktrees) and should never be
# tracked. Each Claude session writes its own subdirectory here.
.claude/
.gstack/
