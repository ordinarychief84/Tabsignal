import { GuestBeaconSkeleton } from "@/components/guest/skeleton";

/** Beacon screen — the dark canvas. Children override with their own. */
export default function Loading() {
  return <GuestBeaconSkeleton />;
}
