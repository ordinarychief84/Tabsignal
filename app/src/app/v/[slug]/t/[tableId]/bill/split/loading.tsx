import { GuestPanelSkeleton } from "@/components/guest/skeleton";

/** Light guest panel — overrides the dark beacon skeleton one level up. */
export default function Loading() {
  return <GuestPanelSkeleton />;
}
